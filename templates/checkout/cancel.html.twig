{% extends 'base.login.html.twig' %}


{% block title %}Paiment annulée
{% endblock %}

{% block body %}

	<div class="flex h-screen w-screen">


		<main id="main" class="flex flex-1">

			<section id="" class="z-10 flex flex-col items-center justify-center bg-gradient-to-r from-slate-900 to-blue-900 w-full">
				<a href="{{ path('default_index') }}">
					<img src="{{ asset('images/navbar/logo_white.svg') }}" alt="LogoAstroShe">
				</a>

				<div class="flex w-3/6 flex-col justify-center">
					<h2 class="text-4xl text-white">Paiment annulée</h2>
					<h3 class="mt-4 text-xl text-white">
						Le paiment a été annulée, veuillez retenté de payer en cliquant sur le bouton dans le mail que vous avez reçu.
					</h3>
					<a href="{{ path('default_index') }}" type="button" class="focus:shadow-outline mt-4 cursor-pointer rounded-md bg-blue-600 px-4 py-2 text-center text-lg font-bold text-white hover:bg-blue-700 focus:outline-none">
						Revenir à l'accueil
					</a>
				</div>
			</section>
		</main>

	</div>
{% endblock %}
