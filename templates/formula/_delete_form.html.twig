<form class="flex justify-start mt-3" method="post" action="{{ path('app_formula_delete', {'id': formula.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formula.id) }}">
	<button class="text-white font-medium  bg-red-500 hover:bg-red-700 transition-all duration-300 ease-out rounded-lg px-3 py-2">Supprimer la formule</button>
</form>
