{% extends 'base.html.twig' %}

{% block title %}Modifier une Catégorie{% endblock %}

{% block body %}
    {% set modal_id = 'categoryEditModal' %}
	{% set modal_content_id = 'categoryEditModalContent' %}


	<!-- Close Button -->
	<button class="absolute top-3 right-3 text-white font-medium bg-red-500 hover:bg-red-700 transition-all duration-300 ease-out rounded-lg px-2 py-1" onclick="toggleModal('categoryEditModal', 'categoryEditModalContent')">
		Fermer
	</button>

	<!-- Contenu de notre modal ici  -->
	<h2 class="text-3xl mb-5 dark:text-white">Modifier une Catégorie</h2>

	
	{{ include('category/_form.html.twig', {'button_label': 'Mettre à jour cette catégorie'}) }}

	{{ include('category/_delete_form.html.twig') }}    
{% endblock %}
