{{ form_start(form, {'action': form_action, 'attr': {'class': 'w-full'}}) }}
{{ form_widget(form) }}
	<button class="text-white font-medium bg-nav-btn hover:bg-blue-800 transition-all duration-300 ease-out rounded-lg px-5 py-1 mt-7 mb-3">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
